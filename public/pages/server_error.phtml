<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?php echo htmlspecialchars($errorTitle ?? 'Error'); ?></title>
    <link rel="stylesheet" href="../style/error.css"> </head>
<body>
<div class="error-container">
    <h1><?php echo htmlspecialchars($errorHeadline ?? 'An Error Occurred'); ?></h1>
    <p class="error-message">
        <?php echo htmlspecialchars($message ?? 'Something went wrong.'); ?>
    </p>
    <div class="error-actions">
        <?php if (isset($showGoBackLink) && $showGoBackLink): ?>
            <a href="javascript:history.back()" class="back-link">Go Back</a>
        <?php endif; ?>
        <a href="/index.php" class="home-link">Homepage</a> </div>
</div>
</body>
</html>